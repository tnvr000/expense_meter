<header>
  <h1>Expense Meter</h1>
</header>
<div>
  <% if user_signed_in? %>
    <%= link_to('Sign Out', sign_out_path, data: { turbo_method: :delete }) %>
  <% else %>
    <%= link_to('Sign In', sign_in_path) %>
  <% end %>
</div>
